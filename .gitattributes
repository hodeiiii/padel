# Normalize line endings to LF in the repository (matches existing history).
* text=auto eol=lf

# Treat known binaries as binary so they are never line-ending converted.
*.xlsx binary
*.ico binary
*.png binary
*.jpg binary
*.woff binary
*.woff2 binary
